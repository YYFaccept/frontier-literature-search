---
name: frontier-literature-search
license: MIT
description: "生成 Google Scholar 顶会顶刊组合检索式，使用可用的 AI 搜索、联网搜索或学术工具检索最新论文、核验发表状态与日期并分类。屏幕控制与 Scholar 页面读取为可选路径。适用于新方向摸底、近几个月论文、相关工作补检或仅生成查询组合；仅排版参考文献时不启用完整检索流程。"
---

# 前沿文献检索

把研究问题转化为有来源、有发表状态、可继续阅读和管理的文献集合。不能用 AI 概述、少量易获取的旧论文或 arXiv 结果代替对最新正式成果的检索。

## 确定检索范围

- 从用户要求和当前课题资料提取研究问题、对象、任务、时间范围及目标会议期刊；只有缺失信息会实质改变检索时才询问。
- 默认重点窗口为当前年份前两年的 1 月 1 日至检索当天，覆盖前两个年度及本年度。例如 2026 年执行时查 2024—2026 年，并注明截至日期；不把未来会议、尚未公布的论文算作已有成果。
- 用户说“近几个月”但未给月数时，默认最近三个日历月，先写明起止日期；“最新”请求在总体窗口内额外检查这一最近三个月窗口。以检索当天为截止，不固定使用本文件示例日期。
- 保留确有必要的经典论文，单列为背景或方法源头。用户指定年份、学科或场所时按其要求，不机械遍历所有会议。
- 用户仅要求生成组合式时，交付适配目标搜索入口的查询及时间条件，不强制打开网页或执行搜索。仅分类已有文献时不擅自扩展检索。用户要求实际检索或测试时，执行可用的搜索路径并核验结果；不把“组合式已生成”写成“检索已完成”。

## 1. AI 搜索快速摸底

使用当前可用的 AI 搜索、研究工具或联网搜索，形成简短背景、关键术语及同义词、候选技术路线和锚点论文。工具返回的摘要和引用先视为线索，打开原始页面后再纳入证据。

检索中明确加入近期年份和相关会议期刊；同时查看该方向当年度最新可用的官方论文集或接收列表，弥补搜索索引滞后。只选与问题相关的场所，使用 [检索指南](references/search-guide.md#目标会议与期刊) 选择目标。

## 2. 生成组合式并执行可用的检索路径

**不强制使用屏幕控制，不要求先读取本地 Google Scholar 页面。** 默认使用当前可用的 AI 搜索、普通联网搜索、学术搜索工具及官方论文目录完成检索；Scholar 页面操作在用户明确指定或当前可用且有帮助时采用。

1. 选择 [五组会议期刊](references/search-guide.md#目标会议与期刊) 中与课题有关的目标，将主题词、同义词、场所与时间条件组合。每个查询保持简短，按场所或技术路线拆分；保留用户要求的 Scholar `source:` 组合作为可复制版本。
2. 区分搜索入口：Scholar 使用其专用组合式或高级检索字段；普通联网搜索使用主题词、会议名称和年份，配合工具支持的域名、日期过滤；学术 API 使用其文档支持的参数。不要把 `source:`、`+` 或复杂 OR 的语义跨搜索工具照搬。示例见 [检索指南](references/search-guide.md#查询方法与示例)。
3. 实际执行时先查近期相关结果，再根据结果补充同义词和具体场所。用官方论文集与预印本平台互相补充：正式成果与新预印本分别标注，不能因一个查询无结果就断言没有研究。
4. 打开候选论文的原始记录核实日期和发表状态。搜索引擎的“最近抓取”“发布时间”和排序只作为线索；详细结论依照下面的新近性核验。
5. 记录实际使用的工具、查询、时间与场所条件及有用结果。生成但没有执行的组合式标为“未执行”；通过其他入口找到论文只证明该入口有结果，不能据此声称 Scholar 的组合语法已验证。

### 可选的 Scholar 页面操作

采用屏幕控制时才读取当前 `computer-use` 技能，定位已打开的目标页面，按新页面状态操作并核对结果。可通过高级检索设置出版物与年份，使用相关性、日期排序及“被引用”“相关文章”补充线索。

页面或控制工具不可用时遵守其停止指令，记录“Scholar 页面未使用／不可用”；这一可选路径缺失本身不阻止其他已授权检索。用户明确要求必须通过本地页面的任务，应如实报告该项未完成，不冒充已完成。遇验证码或访问限制暂停该入口，不自动解验证码、不绕过限制；镜像仅使用用户提供或已打开的入口，不自动轮换重试。

短查询用于控制检索含义、减少漏检和便于调整；不把长查询与封禁写成已证实因果关系，不虚构安全请求频率或字符阈值。

## 3. 核实论文与发表状态

为候选论文打开会议论文集、期刊出版页或官方接收列表，核对标题、作者、年份、场所和状态。参考 [证据与分类模板](references/search-guide.md#证据与分类模板)。

- **正式发表**：有官方论文集条目或期刊出版记录；记录正式年份和 DOI 或官方链接。
- **已接收**：有官方接收名单或明确接收决定，但尚未确认正式出版。投稿页面本身不能证明接收。
- **预印本**：有预印本原始页面；未确认正式发表时明确写“预印本，正式发表未确认”。出现在 arXiv 上不等于没有正式发表。
- **待核验**：只有搜索结果、导读或不完整元数据；列为线索，不能作为已经核实的正式成果。

合并同一工作的预印本与正式版本，以正式条目作为主记录并保留可读版本链接；标题、作者和 DOI 足以判断时直接比较，不创建哈希。不同后续工作不因主题接近而合并。区分首次发布、版本更新与正式发表年份，不能把旧论文新上传或修订的日期当作新成果年份。

### 检验是否搜到最近几个月的论文

逐篇记录能够核实的**首次公开日期、最新版本日期、正式发表或接收日期**，附直接证据；缺失的日期写“未确认”，不以会议举办日、网页抓取日或仅有的年份代替。

- **窗口内首次公开的新工作**：原始版本历史或出版记录显示首次公开落在窗口内；优先作为检索新近性的直接证据。
- **旧工作在窗口内正式发表／接收**：保留较早的预印本日期，单独列为近期发表进展，不称为新出现的研究。
- **窗口内仅更新版本**：归入更新条目，不计作近期新论文。仅有“2026”等年份但无法确认月份时，不纳入三个月窗口的已核实例数。

小规模能力测试以若干实际找到且日期证据完整的论文为依据，说明能找到什么及未覆盖什么；不把找到少数论文等同于覆盖全部最新成果。若目标窗口没有找到合格结果，报告实际结果和查询范围，不能静默扩大时间或换成不相关主题。

研究现状的判断必须落在已读证据上。明确区分论文原文事实、公众号或工具提供的线索、自己的归纳与推断。读不到全文时标注“仅摘要”或“全文未获取”，不编造实验、性能或局限。近期正式成果不足时如实说明已检索的范围，不拿旧论文或预印本凑数并宣称覆盖完整。

## 4. 动态线索与分类管理

按主题选择 [公众号候选名单](references/search-guide.md#微信公众号线索)，使用可访问推文或用户提供的链接发现近期论文。导读中的录用、性能等主张须回到论文或官方页面核对；无法访问公众号时说明本次未覆盖，不冒充已读。

按研究课题建立集合，以技术路线组织子集合，用会议期刊、年份、发表状态和阅读状态作标签。保留用户已有分类；需要会议级别时再查当前官方目录，不能仅凭这份会议清单标为 CCF-A。

默认只交付分类索引。用户需要可导入文件时，提供有核实元数据的 BibTeX 或 RIS；缺失字段留空，不能臆造 DOI、卷期或页码。订阅公众号、建立提醒、创建自动任务或修改 Zotero 库，须有对应的明确请求。

## 交付与完成条件

按任务规模提供三项内容，不要求固定论文数量：

1. **研究现状**：主要技术路线、近期变化、值得先读的论文及依据；推断直接标明。
2. **分类论文表**：标题、作者、年份、场所、发表状态、技术路线、与课题的关系、官方来源及全文获取情况；经典论文与近期成果分开，待核验线索单列。
3. **检索记录**：日期与时间范围、使用的搜索工具、实际查询及筛选、已检查的官方场所、动态线索覆盖；Scholar 组合式是否执行以及是否使用页面。只写实际完成的操作。近几个月检索另列逐篇日期证据与新近性分类。

已有检索覆盖约定的主题和相关近期场所、关键论文证据与版本已厘清后结束；不要求某一种页面或工具必须成功。用户明确要求的步骤若受阻，交付已有结果并说明缺口。核验以发现会改变结论的具体错误为目的；已经核实且没有新增疑点的条目不反复审查。
